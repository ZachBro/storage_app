<div>
  <% if @current_rnr.any? %>
  <%= will_paginate @match_state, :param_name => 'pageRNR' %>
    <%= render "current", current: @current_rnr %>
  <%= will_paginate @match_state, :param_name => 'pageRNR' %>
  <% else %>
    <div class="form-container text-center alert alert-success">
      <p class="no-records">No Room Not Ready Storage at this time</p>
    </div>
  <% end %>
</div>
